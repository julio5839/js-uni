const runFunc = () =>{
    console.log(`߷߷߷`);
};

const runFunc2 = () =>{
    console.log(`:)))`);
};

const status1 = "Completed";

switch(status1) {
    case 'Completed':
        runFunc();
        break;
    case 'Running': 
        runFunc2();
        break;
}